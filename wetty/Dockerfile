FROM wettyoss/wetty:latest

# Set environment variables
ENV WETTY_PORT=3000
ENV WETTY_BASE=/terminal

# Expose port
EXPOSE 3000

# Run wetty with configuration
CMD ["wetty", "--base", "/terminal", "--port", "3000", "--allow-iframe"]
